# Optional browser API origin; use an empty value for same-origin deployments.
NEXT_PUBLIC_API_BASE_URL=
# Server-only backend origin for Next.js API rewrites.
BACKEND_API_ORIGIN=http://backend:4000
# Optional base URL for the Playwright end-to-end test runner.
PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000
